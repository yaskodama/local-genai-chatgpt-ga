# Japanese Source Texts

Put local UTF-8 Japanese `.txt` files in this directory before building the
GA-selected mixed corpus.

```bash
cd local-genai
python build_mixed_corpus.py
```

Use texts whose license allows local training.
